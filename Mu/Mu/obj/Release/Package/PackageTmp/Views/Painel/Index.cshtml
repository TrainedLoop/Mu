@model List<Mu.Repository.Character>
@using Mu.Repository;
@{
    var result = ViewBag.Result;
}
@if (ViewBag.Result != null)
{
    @section scripts{
        <script>
            alert('@ViewBag.Result');
        </script>
    }
}
<div class="col-lg-12 well">
    <h3>Painel de Personagens</h3>
    <div class="col-lg-12">
        <table class="table table-striped table-hover table-condensed text-center">
            <thead>
                <tr>
                    <th style="text-align:center">
                        Nome
                    </th>
                    <th style="text-align:center">
                        Level
                    </th>
                    <th style="text-align:center">
                        Class
                    </th>
                    <th style="text-align:center">
                        Resets
                    </th>
                    <th style="text-align:center">
                        Resetar
                    </th>
                    <th style="text-align:center">
                        Pontos
                    </th>
                    <th style="text-align:center">
                        Time
                    </th>
                    <th style="text-align:center">
                        Deletar
                    </th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                    <tr>
                        <td>
                            @item.Name
                        </td>
                        <td>
                            @item.Clevel
                        </td>
                        <td>
                            @(((CharacterExtensions.Classes)item.Class).ToString().Replace("_", " "))
                        </td>
                        <td>
                            @item.Resets
                        </td>
                        <td>
                            @using (Html.BeginForm("Reset", "Painel", FormMethod.Post))
                            {
                                <input type="hidden" name="CharName" value="@item.Name" />
                                <input type="submit" value="Resetar" />
                            }
                        </td>
                        <td>
                            @using (Html.BeginForm("DistributorPoints", "Painel", FormMethod.Post))
                            {
                                <input type="hidden" name="CharName" value="@item.Name" />
                                <input type="submit" value="Pontos" />
                            }
                        </td>
                        <td>
                            @using (Html.BeginForm("Team", "Painel", FormMethod.Post))
                            {
                                <input type="hidden" name="CharName" value="@item.Name" />
                                <input type="submit" value="Time" />
                            }
                        </td>
                        <td>
                            @using (Html.BeginForm("DeleteChar", "Painel", FormMethod.Post))
                            {
                                <input type="hidden" name="CharName" value="@item.Name" />
                                <input type="submit" value="Deletar" />
                            }
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>
